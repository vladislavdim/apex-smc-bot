services:
  - type: worker
    name: apex-smc-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    autoDeploy: true
    buildFilter:
      ignoredPaths:
        - brain.db
