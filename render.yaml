services:
  - type: worker
    name: apex-smc-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    # Production releases are dispatched only by the verified controlled
    # controlled production release workflow. Database backups never deploy.
    autoDeploy: false
    buildFilter:
      ignoredPaths:
        - brain.db
